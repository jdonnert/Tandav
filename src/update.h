enum Update_Parameters {
	BEFORE_MAIN_LOOP,
	AFTER_NEW_TIMESTEPS,
	BEFORE_FIRST_KICK,
	AFTER_FIRST_KICK,
	BEFORE_DRIFT,
	BEFORE_SNAPSHOT,
	BEFORE_FORCES,
	BEFORE_SECOND_KICK,
	AFTER_SECOND_KICK
};

void Update(enum Update_Parameters stage); 
